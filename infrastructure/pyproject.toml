[project]
name = "infrastructure"
version = "0.1.0"
description = "Infrastructure to support frontend and backend"

readme = "README.md"
requires-python = ">=3.13.2"

dependencies = [
    # Specific to this project

    # Managed by upstream template
    "pyright>=1.1.408",
    "ty>=0.0.17",
    "pytest>=9.0.2",
    "pytest-cov>=7.0.0",
    "pytest-randomly>=4.0.1",
    "pulumi>=3.223.0",
    "pulumi-aws>=7.20.0",
    "pulumi-aws-native>=1.54.0",
    "pulumi-command>=1.1.3",
    "ephemeral-pulumi-deploy>=0.0.6",
    "lab-auto-pulumi>=0.1.18",
    "boto3>=1.42.53",
    "boto3-stubs[all]>=1.42.53",
]

[tool.uv]
package = true
